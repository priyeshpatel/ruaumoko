#!/usr/bin/env python

import sys
from os.path import abspath, split, join
sys.path.append(join(split(abspath(__file__))[0], '..'))

from elevation import config, Downloader

if len(sys.argv) == 2:
    cfg = config.load_config(sys.argv[1])
elif len(sys.argv) <= 1:
    cfg = config.default_config()
else:
    print "usage: download-elevation-data [CONFIG_FILE]"
    sys.exit(2)

print "Using configuration file {0}.".format(filename)
downloader = Downloader(cfg)
downloader.run()
